#This file contains values that are passed around the program. Defining them here and importing/editing them in this
#file is a simple solution to avoid complicated passing inside/between modules

#Stores the username and password of the user currently logged in
CurrentUserDetails = ["Username","Password"]

#Stores the click increment value, initialised as 1
ClickIncrementsValue = float(1)

#Stores the number of times that the shooting profiles screen has been entered
ProfilesScreenEntryCount = 0